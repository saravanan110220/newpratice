A simple calculator program in C is a basic program that takes two numbers and an operator as input, performs the corresponding arithmetic operation, and displays the result.

It usually supports operations like:

Addition (+)
Subtraction (-)
Multiplication (*)
Division (/)
